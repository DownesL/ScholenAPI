const express = require('express');
const mainController = require('./../nodeFiles/mainController');

const router = express.Router();

router
    .route('/')
    .get(mainController.getReviews)
    .post(mainController.postReview);

module.exports = router;